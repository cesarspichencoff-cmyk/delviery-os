# Memória do turno

`src/copiloto/shift-memory.js` + `schemas/copiloto/shift-memory.schema.json`

## Separação epistêmica
fato | inferência | relato humano | hipótese | padrão | regra validada | ausente | desconhecido

Nunca promover hipótese a fato automaticamente.
