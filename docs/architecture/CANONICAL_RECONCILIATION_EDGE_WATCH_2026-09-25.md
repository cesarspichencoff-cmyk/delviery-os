# Canonical Reconciliation — Edge vs Gerencial Watch / César Context Kernel

Date: 2026-09-25

## Discovery

A VÉRTICE Difference Check found that a mature Gerencial Watch / César Context
Kernel already exists outside this DeliveryOS branch.

Recovered project evidence includes:

- Gerencial Watch portable v0.10 with Manager Investigator, Evidence Debt,
  prospective capture and Human Context Coverage;
- Gerencial Watch portable v0.11 with server-side Snapshot Engine;
- a separate César Context Kernel Python lineage with SQLite event spine,
  restart persistence, mode engine, commitments, investigator and attention;
- an Apple Watch/Shortcuts route already designed around a single TATÁ command;
- read-only live-source receipts for Gmail/iFood and Calendar.

Therefore continuing to grow a second higher-layer Context Kernel inside
DeliveryOS would duplicate a stronger existing organism.

## Failure class

```text
LOCAL_CONTINUATION_WITHOUT_DIFFERENCE_CHECK
    -> DUPLICATE_HIGHER_LAYER_KERNEL
```

## Circuit breaker

The route "DeliveryOS becomes the canonical César Context Kernel" is blocked.

From this point:

```text
DeliveryOS / TATÁ Edge
    owns:
      operational observation
      durable journal
      identity projection
      hard-exception candidates
      Edge Manager Snapshot
      versioned Watch handoff contract

Gerencial Watch / César Context Kernel
    owns:
      voice / universal capture
      work-personal modes
      commitments
      evidence debt
      Manager Investigator
      Human Context Coverage
      Snapshot Engine
      source freshness / critical coverage
      Attention Governor
      Apple Watch interaction
```

## Status of src/contextKernel in this branch

The TypeScript modules created here are retained only as:

`EXPERIMENTAL_COMPATIBILITY_HARNESS / NON_CANONICAL`

They have synthetic value for contract tests, but they are not the product
runtime and must not be deployed as a competing brain.

No deletion is performed yet because the harness is useful for proving
cross-language semantics and because deletion without parity evidence would
discard tested work.

## Bridge rule

The new `EdgeWatchHandoffEnvelope` is deliberately transport-neutral.

It does not guess the current Gerencial Watch HTTP/D1 schema.

A concrete adapter may be added only after the exact current portable runtime
contract is recovered or exposed by the canonical Watch runtime.

## Freshness invariant transferred from Watch v0.11

```text
SNAPSHOT_FRESH != SOURCE_FRESH
COMPUTED_NOW != OBSERVED_NOW
```

The Edge handoff therefore carries its source watermark separately from its
generation time and does not claim global coverage.

## Next highest-value work

1. prove the Edge handoff contract in cloud CI;
2. recover the exact current Gerencial Watch v0.11 input contract;
3. implement one adapter from Edge handoff -> canonical Watch runtime;
4. reuse the existing mail bridge and Snapshot Engine rather than create new
   mail/snapshot infrastructure;
5. keep physical PC binding last.
