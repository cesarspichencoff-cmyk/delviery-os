# Context Kernel TypeScript Harness

## Status

`EXPERIMENTAL_COMPATIBILITY_HARNESS / NON_CANONICAL`

This directory is **not** the canonical Gerencial Watch / César Context Kernel.

A VÉRTICE Difference Check recovered a more mature existing Gerencial Watch
lineage (portable v0.10/v0.11) with voice capture, Manager Investigator,
Evidence Debt, Human Context Coverage and a server-side Snapshot Engine.

These TypeScript modules are kept only to:

- test Edge -> higher-layer semantics;
- prove truth/source-mode boundaries;
- exercise mode/commitment/attention contracts synthetically;
- support future parity tests with the canonical runtime.

Do not deploy this directory as a second personal/managerial brain.

Canonical ownership:

- `src/edge/*` -> TATÁ operational Edge;
- Gerencial Watch / César Context Kernel portable runtime -> higher-layer brain.
